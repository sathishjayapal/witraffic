
spring:
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888/}
  application:
    name: wi-traffic-alerts
  devtools:
    livereload:
      port: 8005
  cloud:
    config:
      uri: http://localhost:8888
      profile: dev
  jpa:
    show-sql: true
  datasource:
    url: jdbc:h2:mem:testdb
  h2:
    console:
      enabled: true
server:
  port: 8001
